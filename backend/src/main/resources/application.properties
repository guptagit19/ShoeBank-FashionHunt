# Application Name
spring.application.name=nepal-shop

# Server Configuration
server.port=${PORT:8080}

# MySQL Database Configuration
spring.datasource.url=${DATABASE_URL:jdbc:mysql://localhost:3306/nepal_shop?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true}
spring.datasource.username=${DATABASE_USERNAME:root}
spring.datasource.password=${DATABASE_PASSWORD:root}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver


# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=${DDL_AUTO:update}
spring.jpa.show-sql=${SHOW_SQL:true}
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.globally_quoted_identifiers=true

# Connection pool settings for cloud MySQL
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.maximum-pool-size=5

# JWT Configuration
jwt.secret=${JWT_SECRET:ShoeBankFashionHuntNepalSecretKey2026VerySecureKeyForJWTToken}
jwt.expiration=86400000

# File Upload Configuration
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Upload Directory
app.upload.dir=uploads

# Cloudinary Configuration
cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME:dqbpxyv7q}
cloudinary.api-key=${CLOUDINARY_API_KEY:257351416991965}
cloudinary.api-secret=${CLOUDINARY_API_SECRET:UgZ13uYsWOa2iKpU84RFP6EgYcI}

# CORS Configuration
app.cors.allowed-origins=${CORS_ORIGINS:*}

# eSewa Configuration (Sandbox)
esewa.merchant-id=${ESEWA_MERCHANT_ID:EPAYTEST}
esewa.secret-key=${ESEWA_SECRET_KEY:8gBm/:&EnhH.1/q}
esewa.payment-url=https://rc-epay.esewa.com.np/api/epay/main/v2/form
esewa.verify-url=https://rc.esewa.com.np/api/epay/txn/verify
esewa.success-url=${ESEWA_SUCCESS_URL:http://localhost:5173/payment/success}
esewa.failure-url=${ESEWA_FAILURE_URL:http://localhost:5173/payment/failure}

# Logging
logging.level.com.shoebank=${LOG_LEVEL:DEBUG}
logging.level.org.springframework.security=${LOG_LEVEL:DEBUG}
